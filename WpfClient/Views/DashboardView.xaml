<UserControl x:Class="WpfClient.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <UniformGrid Columns="4" Margin="0,0,0,20">
            <Border Background="#FFFFFF" BorderBrush="#D9E1EA" BorderThickness="1" CornerRadius="6" Padding="18" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Нові" Foreground="#687789" />
                    <TextBlock Text="8" FontSize="30" FontWeight="SemiBold" Foreground="#17202B" />
                </StackPanel>
            </Border>
            <Border Background="#FFFFFF" BorderBrush="#D9E1EA" BorderThickness="1" CornerRadius="6" Padding="18" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="В роботі" Foreground="#687789" />
                    <TextBlock Text="5" FontSize="30" FontWeight="SemiBold" Foreground="#17202B" />
                </StackPanel>
            </Border>
            <Border Background="#FFFFFF" BorderBrush="#D9E1EA" BorderThickness="1" CornerRadius="6" Padding="18" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Відправлені" Foreground="#687789" />
                    <TextBlock Text="3" FontSize="30" FontWeight="SemiBold" Foreground="#17202B" />
                </StackPanel>
            </Border>
            <Border Background="#FFFFFF" BorderBrush="#D9E1EA" BorderThickness="1" CornerRadius="6" Padding="18">
                <StackPanel>
                    <TextBlock Text="Оплачені" Foreground="#687789" />
                    <TextBlock Text="11" FontSize="30" FontWeight="SemiBold" Foreground="#17202B" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#D9E1EA" BorderThickness="1" CornerRadius="6" Padding="20">
            <StackPanel>
                <TextBlock Text="Поточний стан клієнта" FontSize="18" FontWeight="SemiBold" Foreground="#17202B" />
                <TextBlock Text="Поки що показані демонстраційні дані, щоб WPF-клієнт запускався автономно."
                           TextWrapping="Wrap"
                           Foreground="#687789"
                           Margin="0,8,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
